<div class="film-grain" aria-hidden="true"></div>

<style>
  .film-grain {
    position: absolute;
    inset: -28%;
    z-index: 1;
    pointer-events: none;
    opacity: var(--grain-opacity, .055);
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 320 320' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.86' numOctaves='4' seed='7' stitchTiles='stitch'/%3E%3CfeColorMatrix type='saturate' values='0'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='.7'/%3E%3C/svg%3E");
    background-repeat: repeat;
    background-size: var(--grain-size, 360px) var(--grain-size, 360px);
    mix-blend-mode: soft-light;
    transform: translate3d(-2%, -2%, 0) scale(1.04);
    animation: film-grain-shift var(--grain-speed, 9s) steps(8, end) infinite;
    will-change: transform;
  }

  @keyframes film-grain-shift {
    0% { transform: translate3d(-2%, -2%, 0) scale(1.04); }
    20% { transform: translate3d(2%, -1%, 0) scale(1.045); }
    40% { transform: translate3d(-1%, 2%, 0) scale(1.04); }
    60% { transform: translate3d(3%, 1%, 0) scale(1.05); }
    80% { transform: translate3d(0, -3%, 0) scale(1.045); }
    100% { transform: translate3d(-2%, -2%, 0) scale(1.04); }
  }

  @media (max-width: 720px) {
    .film-grain {
      inset: -20%;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .film-grain {
      opacity: .032;
      animation: none;
      transform: none;
      will-change: auto;
    }
  }
</style>
