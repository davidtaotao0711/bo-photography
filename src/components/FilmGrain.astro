<div class="grain-overlay" data-grain-overlay aria-hidden="true"></div>

<style>
  .grain-overlay {
    position: absolute;
    inset: 0;
    z-index: 2;
    overflow: hidden;
    pointer-events: none;
    opacity: var(--grain-opacity, .09);
    mix-blend-mode: soft-light;
  }

  .grain-overlay::before,
  .grain-overlay::after {
    content: '';
    position: absolute;
    inset: -32%;
    pointer-events: none;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='grain' x='-20%25' y='-20%25' width='140%25' height='140%25'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.92' numOctaves='4' seed='13' stitchTiles='stitch'/%3E%3CfeColorMatrix type='saturate' values='0'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23grain)' opacity='.95'/%3E%3C/svg%3E");
    background-repeat: repeat;
    background-size: var(--grain-size, 220px) var(--grain-size, 220px);
    filter: contrast(185%) brightness(106%);
    transform: translate3d(-2%, -2%, 0);
    animation: grain-shift var(--grain-speed, 1.6s) steps(10, end) infinite;
    will-change: transform;
  }

  .grain-overlay::after {
    opacity: .42;
    background-size:
      var(--grain-size-secondary, 139px)
      var(--grain-size-secondary, 139px);
    transform: translate3d(2%, 1%, 0) rotate(90deg);
    animation-name: grain-shift-secondary;
    animation-direction: reverse;
  }

  @keyframes grain-shift {
    0% { transform: translate3d(-2%, -2%, 0); }
    20% { transform: translate3d(2%, -1%, 0); }
    40% { transform: translate3d(-1%, 2%, 0); }
    60% { transform: translate3d(3%, 1%, 0); }
    80% { transform: translate3d(0, -3%, 0); }
    100% { transform: translate3d(-2%, -2%, 0); }
  }

  @keyframes grain-shift-secondary {
    0% { transform: translate3d(2%, 1%, 0) rotate(90deg); }
    25% { transform: translate3d(-1%, -2%, 0) rotate(90deg); }
    50% { transform: translate3d(1%, 3%, 0) rotate(90deg); }
    75% { transform: translate3d(-3%, 0, 0) rotate(90deg); }
    100% { transform: translate3d(2%, 1%, 0) rotate(90deg); }
  }

  @media (prefers-reduced-motion: reduce) {
    .grain-overlay {
      opacity: .05;
    }

    .grain-overlay::before,
    .grain-overlay::after {
      animation: none;
      will-change: auto;
    }
  }
</style>
